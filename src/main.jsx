import React from 'react';
import ReactDOM from 'react-dom/client';
import WasteSortGame from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WasteSortGame />
  </React.StrictMode>
);
